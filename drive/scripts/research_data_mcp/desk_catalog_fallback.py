#!/usr/bin/env python3
"""Inventory wallpaper fallback — STRIPPED.

Former regex brain that answered Ask from vault_brief token matches.
Composer + research_discover_desk own holdings questions.
"""

from __future__ import annotations

from typing import Any


def try_inventory_fallback(
    message: str,
    vault_brief: str,
    *,
    repo_root: Any = None,
) -> str | None:
    _ = message, vault_brief, repo_root
    return None
