[pytest]
testpaths = tests drive/tests
python_files = test_*.py
addopts = -ra -m "not slow"
pythonpath = . kernel drive
markers =
    slow: measures the real registry's bytes; needs RESEARCH_DATA_ROOTS and minutes, not seconds
